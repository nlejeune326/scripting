using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HelloWorld2 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        print("j'ai fait caca");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
